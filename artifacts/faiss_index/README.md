# FAISS Index Output

Ce dossier est le point de sortie de la commande `python app.py build-index`.

Fichiers attendus apres build:

- `index.faiss`
- `index.pkl`
- `index_metadata.json`

Les fichiers binaires d'index sont ignores par git pour garder le depot leger.
